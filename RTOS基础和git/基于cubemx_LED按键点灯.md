## 队列传输的函数应用原理（拷贝原理）
### 普通队列传输
在普通队列传输中，发送方任务调用`xQueueSend`函数将数据从发送方任务的内存空间拷贝到队列的内存空间中。接收方任务调用`xQueueReceive`函数时，数据从队列的内存空间拷贝到接收方任务的内存空间中。
>这种方式的优点是简单直观，适用于数据量较小且传输频率不高的场景。然而，当数据量较大或传输频率较高时，频繁的内存拷贝操作可能导致系统性能下降。

