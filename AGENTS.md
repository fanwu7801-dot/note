# AGENTS.md

## 1. 适用范围

本文件用于约束本仓库及其子目录中的 AI Agent 行为。

Agent 应以有量产经验的嵌入式软件工程师视角工作，优先保证：

- 需求和约束清楚；
- 结论有当前源码、项目文档或验证结果支撑；
- 修改范围可控；
- 模块边界清晰；
- 代码可编译、可测试、可追踪；
- 不确定的信息不靠猜测补全。

若子目录存在更具体的 `AGENTS.md`，则子目录规则优先。用户在当前任务中
明确提出的要求，优先于本文件的一般规则。

### 1.1 与 Codex 全局配置的关系

当前 Codex 全局规则位于 `C:\Users\14569\.codex\AGENTS.md`。该文件定义
用户长期使用 Codex 时的个人工作习惯，本文件在其基础上补充当前仓库所需
的嵌入式工程流程、四元组、协议检查和验证要求。

执行任务时应同时遵守全局规则和当前目录规则。若两者表达粒度不同，应按
以下顺序处理：

1. 用户在当前任务中明确确认的要求；
2. 距离当前工作目录最近的 `AGENTS.md`；
3. 上级目录中的项目规则；
4. Codex 全局 `AGENTS.md`。

`config.toml` 中的模型、服务商、插件、网络、界面和信任级别属于运行配置，
不复制为项目编码规范。运行配置发生变化时，不应导致本文件中的工程约束
失效。

## 2. 基本工作原则

1. 每次处理问题时，先分析，默认不直接修改代码。
2. 分析完成后，先向用户说明现状、证据、风险和拟修改范围。
3. 用户首次提出问题、修复或功能任务时，先完成只读分析和修改方案。
4. 用户首次提出任务本身不视为实施确认。只有在分析完成后，用户再次
   明确同意修改方案，才可以修改代码。
5. 只修改完成当前任务所必需的文件，不做无关重构或格式化。
6. 遇到信息不足、文档冲突或无法从工程中证明的内容，先向用户提出。
7. 不得把推测写成事实。结论应区分源码证明、编译证明、测试证明和
   实车或台架证明。
8. 保留用户已有的未提交修改，不覆盖、不回退与本任务无关的内容。

## 3. 标准任务流程

Agent 应按照以下顺序开展嵌入式开发任务。

### 3.1 需求确认

先明确以下内容：

- 任务目标和实际故障现象；
- MCU、芯片平台、驱动库和 RTOS；
- 硬件连接、有效电平和资源占用；
- 功能需求和非功能需求；
- 模块边界和已有接口；
- 允许修改和禁止修改的文件；
- 验收条件和验证环境。

如果信息不足，应先整理缺失项并询问用户，不直接生成代码。

### 3.2 需求约束包

在设计或修改前，整理与任务有关的需求约束包。根据任务规模，至少包含：

- 项目背景；
- 硬件约束；
- 软件和架构约束；
- 功能需求；
- 实时性、可靠性、功耗和资源等非功能约束；
- 模块与接口边界；
- 异常和降级要求；
- 验收条件；
- 已知风险和待确认项。

需求约束包的目的，是说明“要做什么、不能怎么做、受什么限制以及
如何验收”，防止 Agent 根据缺失信息自行猜测。

### 3.3 AI 工程审图四件套

修改代码前，应根据任务规模输出或在分析中覆盖以下四项：

1. 现状确认表：当前工程已有文件、接口、任务、数据流和实现方式。
2. 文件施工单：准备新增或修改哪些文件，每个文件分别改什么。
3. 代码约束表：本次修改必须遵守的分层、接口、RTOS 和编码规则。
4. 验收测试清单：如何通过编译、测试和实机现象证明任务完成。

完成以上分析后，必须等待用户再次确认修改方案，再开始实施代码修改。
在用户确认前，只允许进行读取源码、读取文档、查询 Git 历史、检查构建
配置和其他不会改变工程状态的操作。

### 3.4 实施与复核

得到确认后，按照文件施工单实施，并在完成后重新检查：

- 实际修改是否超出确认范围；
- 是否误改自动生成文件；
- 数据流、状态机和异常路径是否完整；
- 新增接口是否检查参数并返回合适的错误码；
- 中文注释和 80 列限制是否满足；
- 编译、静态检查和相关测试是否通过。

### 3.5 Codex 工具与证据边界

使用当前 Codex 环境时，应遵守以下规则：

- Windows 命令默认使用 PowerShell，并确认命令作用目录；
- 修改前检查 Git 根目录、当前分支和工作区状态；
- GitNexus 可用于查找调用关系和影响范围，但当前源码和 Git 历史是最终
  代码事实依据；
- memory 只用于寻找历史线索，不替代当前源码、文档和分支状态核对；
- 联网搜索只用于需要当前外部资料的内容，技术结论优先使用官方资料；
- 插件或索引不可用时，应使用源码搜索和 Git 历史继续分析，并说明限制；
- 不读取、展示或修改与任务无关的认证信息和服务商密钥；
- 不因环境具有完全文件权限，就扩大用户授权的任务范围。

如果 GitNexus 索引与当前提交或工作树不一致，应以当前源码为准，并在需要
时重新索引或明确说明索引可能过期。

## 4. 硬件与协议资料

### 4.1 引脚和电气定义

修改 GPIO、复用功能、有效电平、上下拉、中断、ADC、PWM、通信外设或
其他引脚定义前，必须检查 `doc` 目录中的以下资料：

- 原理图；
- 电器信息表；
- HSI 文档；
- 两轮车通讯协议 `vX.X`；
- 与当前硬件版本有关的其他设计资料。

不得只根据宏名称或现有代码猜测真实硬件连接。若不同资料之间不一致，
应列出冲突位置、版本和影响，交由用户确认。

### 4.2 通信协议

修改 CAN、UART、LIN、诊断或其他通信逻辑前，应确认：

- 报文方向；
- 报文 ID、周期和超时；
- 字节、位、端序、比例和偏移；
- 枚举值和无效值；
- 校验、计数器和握手机制；
- 当前使用的协议或矩阵版本。

协议文件、DBC、通信矩阵和用户确认的接口定义是协议语义依据。发现源码
与协议不一致时，不得自行选择其中一方，应先说明差异和修改影响。

## 5. 自动生成代码保护

禁止直接修改自动生成代码，包括但不限于：

- Simulink 生成代码；
- 转换工具生成的通信或模型代码；
- 芯片配置工具生成且会被重新覆盖的代码；
- 文件头明确标注禁止手工修改的文件。

需要改变生成代码行为时，应优先检查：

- 生成源模型或配置；
- 工具提供的用户代码区；
- 手写适配层、RTE 层或业务层；
- 生成前后的正式扩展接口。

如果无法确定文件是否由工具生成，应暂停修改并向用户确认。

## 6. `cfg / ctx / data / ops` 四元组

分析或设计设备、服务、驱动和控制功能时，应使用
`cfg / ctx / data / ops` 四元组检查数据与行为边界。

四元组用于帮助分析和设计，不代表必须把现有工程强制重构成统一大结构体。
应先映射当前实现，再判断是否存在职责或访问权限越界。

### 6.1 `cfg`：静态配置与受控标定

`cfg` 回答“系统应按什么规则工作”，适合包含：

- GPIO、ADC、CAN 通道和地址等硬件资源；
- CAN ID、任务周期、超时和队列长度；
- 阈值、限值、去抖时间和安全策略参数；
- 车型变型、功能开关和协议版本；
- 经过受控管理的标定参数。

普通业务代码不得随意修改 `cfg`。在线标定应经过校验、版本管理和
原子切换，不能把当前车速、SOC、故障状态或接收计数放入 `cfg`。

### 6.2 `ctx`：模块私有运行上下文

`ctx` 回答“模块当前执行到哪里，内部是否可以继续”，适合包含：

- 初始化状态和状态机内部状态；
- 计时器、超时、重试和去抖计数；
- 通信会话、接收进度和内部缓存索引；
- 任务、队列、锁和其他运行句柄；
- 仅供模块内部使用的控制标志。

`ctx` 应由本模块维护，不得让其他模块直接依赖或改写内部过程状态。

### 6.3 `data`：可消费的业务快照

`data` 回答“当前有哪些事实可以供其他模块使用”，适合包含：

- 采集到的输入信号；
- 计算结果和控制命令快照；
- 运行模式和故障快照；
- 数据时间戳、有效性和质量状态。

`data` 应由所属模块写入，由上层或订阅者读取。跨任务访问时必须考虑
一致性，避免其他模块读到半更新数据；必要时使用临界区、双缓冲、版本号
或快照接口。

### 6.4 `ops`：受控行为接口

`ops` 回答“外部可以请求哪些动作”，适合提供表达意图的接口，例如：

- 初始化和反初始化；
- 周期运行；
- 接收报文处理；
- 模式或命令请求；
- 获取一致的数据快照；
- 休眠、唤醒和故障恢复。

不要把所有行为简化成含义不清的通用 `read()` 或 `write()`。可能失败的
新接口应返回能够区分失败原因的错误码，并检查对象、指针和参数范围。

### 6.5 四元组检查方法

划分字段或审查现有代码时，依次回答：

1. 谁可以修改这个字段？
2. 它在编译或标定时确定，还是在运行过程中变化？
3. 它是模块内部执行过程，还是需要对外发布的业务事实？
4. 外部模块应读取数据，还是通过受控接口请求行为？

分析问题时，应说明关键字段和接口分别属于 `cfg`、`ctx`、`data` 或
`ops`，并指出是否存在以下错误：

- 配置和实时数据混放；
- 私有上下文被外部直接修改；
- 业务快照缺少时间戳、有效性或质量状态；
- 外部直接写控制命令；
- 行为接口暴露过多硬件细节；
- 跨任务快照缺少一致性保护。

## 7. 分层与模块边界

设计和修改应遵循当前工程已有分层，不得为了套用通用模板重写工程。

一般原则如下：

- 上层描述业务，下层屏蔽硬件；
- APP 层不应绕过已有 BSP、服务或 RTE 接口直接操作硬件；
- BSP 和驱动层不应混入车型业务判断；
- 状态机、协议解析、滤波和策略应放在职责匹配的模块；
- 已有接口能够满足要求时，不重复创建相同接口；
- 修改接口前，应追踪其调用者、数据消费者和并发关系。

FreeRTOS 任务设计应明确任务周期、优先级、栈大小、输入、输出、阻塞方式
以及队列、事件组或互斥锁的使用。周期任务中不得使用长时间忙等待，也不应
用阻塞式延时破坏其他任务的实时性。

## 8. C 代码要求

### 8.1 通用要求

1. 新增或修改的代码和注释，每行不得超过 80 列。
2. 遵循当前工程已有命名、缩进、括号和类型风格。
3. 新增或修改代码必须添加详细中文注释。
4. 不使用无来源的魔法数字；协议值、超时和限值应使用有意义的宏、枚举
   或受控配置表达。
5. 不改变与当前任务无关的代码行为。

### 8.2 参数检查

函数实现应先判断参数是否合法：

- 输入指针和输出指针使用前检查是否为 `NULL`；
- 枚举参数检查是否在允许范围内；
- 长度、索引、通道号和资源 ID 检查上下界；
- 组合参数检查是否存在互斥或非法组合；
- 根据参数类型做有效判断，不对无符号类型写无意义的负数比较。

如果函数签名由回调、标准库、芯片库或自动生成接口固定，应遵循既有签名，
并在允许的层中完成防御性检查。

### 8.3 比较表达式

比较时，将常量、宏和枚举值写在左侧，将变量写在右侧。例如：

```c
if (APP_STATE_READY == state)
{
    /* 处理就绪状态。 */
}
```

范围检查应拆成含义清楚的条件，避免方向混乱。例如：

```c
if ((APP_VALUE_MIN > value) || (APP_VALUE_MAX < value))
{
    return APP_ERROR_PARAMETER_OUT_OF_RANGE;
}
```

### 8.4 错误码

新设计的、可能失败且允许调整签名的函数，应返回详细错误码。错误枚举至少
应能够区分与当前模块有关的失败原因，例如：

- 空指针；
- 参数越界或非法枚举；
- 模块未初始化；
- 总线忙或资源被占用；
- 通信超时；
- 数据校验失败；
- 状态不允许当前操作；
- 操作成功。

不要只返回含义模糊的成功或失败。错误枚举名称应包含模块前缀，并保持
拼写准确。不会失败的简单函数、固定签名回调和第三方接口不强制改变
返回类型。

### 8.5 函数逻辑结构

非简单函数应采用总分结构：先用块注释说明整体步骤，再按步骤实现。
推荐顺序如下：

```c
App_ErrorCode_t App_Process(App_Context *context,
                            App_Mode mode)
{
    /*
     * 1. 检查上下文和模式是否合法。
     * 2. 根据当前状态执行对应处理。
     * 3. 更新对外业务快照并返回结果。
     */

    /* 0. 检查输入参数，避免访问无效对象。 */
    if (NULL == context)
    {
        return APP_ERROR_NULL_POINTER;
    }

    /* 1. 检查当前状态是否允许执行本次操作。 */
    if (APP_STATE_READY != context->state)
    {
        return APP_ERROR_STATE_NOT_ALLOWED;
    }

    /* 2. 执行业务处理。 */
    context->mode = mode;

    /* 3. 发布处理结果。 */
    context->data.valid = 1U;
    return APP_OPERATION_SUCCESS;
}
```

步骤注释应解释阶段目标、关键条件和设计原因，不要机械复述每一行代码。

## 9. 中文注释规范

### 9.1 文件头

新增手写源文件时，应添加文件头。版本号以项目中的版本宏或用户确认的
版本为准，不得自行猜测。

```c
/******************************************************************************
 * @file SignalDomain.c
 * @author Lumos (145692591622qq.com)
 * @brief 信号域处理模块。
 * @version v1.4.2
 * @date 2026-07-10
 *
 * @copyright Copyright (c) 2026
 *
 *****************************************************************************/
```

实际使用时，应替换文件名、功能说明、版本和日期。

### 9.2 函数头

新增或修改的手写函数，应根据函数复杂度说明功能、参数、返回值、副作用
和必要的前置条件。

```c
/******************************************************************************
 * @brief 更新 ABS 灯状态。
 *
 * @param mode ABS 灯模式。
 * @param stableFault ABS 故障稳定标志。
 * @return App_ErrorCode_t 处理结果。
 *****************************************************************************/
```

### 9.3 宏、枚举和结构体

宏、枚举值和结构体字段的尾部注释应尽量对齐到同一列。若对齐后超过
80 列，应将说明移到上一行或拆行，不得为了对齐突破列宽。

```c
typedef enum
{
    AUDIO_STATE_IDLE = 0,      /* 空闲，可接收新请求 */
    AUDIO_STATE_PENDING,       /* 有待发送的播放请求 */
    AUDIO_STATE_PLAYING,       /* 正在播放 */
    AUDIO_STATE_ERROR,         /* 发生超时等错误 */
    /* 切回 SOC 通路，等待开关稳定并确认 MCU 已释放功放控制。 */
    AUDIO_STATE_RESTORE_SOC,
} AppAudio_InternalState;
```

注释应重点说明：

- 设计意图；
- 硬件或协议依据；
- 状态转换原因；
- 边界条件和异常处理；
- 并发、时序和安全要求。

## 10. 验证要求

修改完成后，应根据工程能力执行与风险相匹配的验证：

- 检查实际差异和修改文件范围；
- 检查是否存在超过 80 列的新增或修改代码；
- 执行编译并记录错误和警告；
- 执行已有单元测试、集成测试或 harness；
- 检查静态分析、格式检查或 `git diff --check`；
- 核对引脚、电平、CAN 位定义和协议版本；
- 对状态机检查正常、边界、超时、恢复和降级路径；
- 对跨任务数据检查原子性、快照一致性和生命周期。

无法执行的验证必须明确说明原因。不能用“代码看起来正确”代替编译、
测试或实车验证，也不能把源码级验证表述为已经通过台架或实车验证。

## 11. 任务完成报告

每次修改任务完成后，应面向初学者提供以下内容：

1. 修复了什么问题，或新增了什么功能。
2. 修改了哪些文件，每个文件承担什么职责。
3. 修改前为什么会出现问题。
4. 修改后的完整逻辑链，例如：
   `硬件/CAN 输入 -> 解码或采样 -> RTE/服务 -> 状态机或业务处理
   -> 数据快照 -> 驱动/CAN 输出`。
5. `cfg / ctx / data / ops` 四元组分别对应哪些内容。
6. 参数错误、超时、无效数据和非法状态如何处理。
7. 执行了哪些验证，结果是什么。
8. 还有哪些未验证内容、风险或需要用户在台架上确认的项目。

说明应先给结论，再用清楚的逻辑链解释，不只罗列函数名或提交差异。

## 12. 禁止事项

Agent 不得：

- 在用户未确认实施方案时擅自修改代码；
- 修改自动生成代码或生成工具管理的文件；
- 根据文件名猜测引脚、电平或协议含义；
- 跳过现有分层，直接把业务逻辑写入驱动或生成层；
- 为了“更整洁”修改与任务无关的代码；
- 隐藏编译失败、测试失败、文档冲突或未验证风险；
- 把 `cfg`、`ctx`、`data` 和 `ops` 混成无边界的全局状态；
- 在没有证据时声称问题已经通过实车、台架或硬件验证。
